# SEO and Indexing Plan

## 방향

minjokim.kr은 홍보 사이트가 아니라 공개 연구노트다. SEO의 목적은 검색 유입 자체보다, 연구 글이 Google, Naver, 그리고 AI 검색 시스템에서 정확한 저자, 주제, 발행일, 문맥으로 해석되도록 하는 것이다.

## 날짜 원칙

- 글 상세 페이지에는 발행일을 표시한다.
- 첫 화면의 최신 글에는 글이 3개 이상 쌓인 뒤 작은 날짜 표기를 검토한다.
- Markdown frontmatter에는 `published`, `created`, `description`, `author`, `tags`를 반드시 둔다.
- 단순 수정일보다 최초 발행일을 우선 노출한다.

## 필수 메타데이터

각 발행 글은 아래 항목을 갖는다.

- `title`: 검색 결과 제목
- `description`: 검색 결과 설명과 AI 요약의 기준 문장
- `published`: 최초 발행일
- `author`: 김민조
- `tags`: 연구 범주와 핵심 이론어

## 사이트 수준 신호

- `sitemap.xml`: Quartz가 자동 생성한다.
- `index.xml`: Quartz가 RSS로 자동 생성한다.
- `robots.txt`: 루트에 배치하고 sitemap 위치를 명시한다.
- `canonical`: 각 페이지의 대표 URL을 명시한다.
- `JSON-LD`: 홈은 `WebSite`, 글은 `Article`, 저자 소개 페이지는 `ProfilePage` 구조화 데이터를 사용한다.
- `author.url`: 모든 글의 저자는 `https://minjokim.kr/about`으로 연결한다.
- `author.sameAs`: 네이버 블로그, 브런치, YES24 저자 페이지를 같은 저자 신호로 묶는다.
- `/about`: 김민조 저자 엔티티의 기준 페이지로 사용한다.
- `/research`: 최신순 목록뿐 아니라 주제별 내부 링크 허브로 사용한다.

## 내부 링크 원칙

- 홈에서는 김민조 소개, 연구 글 목록, 논문과 저서로 가는 길을 유지한다.
- `/about`은 김민조, 연구 주제, 논문과 저서, 외부 프로필을 한곳에 묶는다.
- `/research`는 최신 글 목록만 두지 않고 전략 실행, AI와 조직, 조직학습, 가치와 고객 같은 주제 묶음을 둔다.
- 새 글을 발행할 때 같은 주제 묶음 안의 기존 글을 1개 이상 연결한다.
- 내부 링크의 앵커 텍스트는 "여기", "관련 글"보다 글의 주제와 개념이 드러나는 문장으로 쓴다.

## 검색 등록 운영

1. Google Search Console에 `https://minjokim.kr` 등록
2. 소유권 확인 메타태그 또는 DNS TXT 발급 후 반영
3. `https://minjokim.kr/sitemap.xml` 제출
4. Naver Search Advisor에 사이트 등록
5. Naver 소유확인 메타태그 또는 HTML 파일 반영
6. Naver에 `sitemap.xml`과 `robots.txt` 검사 제출
7. 주요 글 발행 후 수집 요청

## 발행 운영 루틴

새 원문을 발행할 때는 아래 순서를 기본값으로 둔다.

1. 원문을 `minjokim.kr`에 먼저 발행한다.
2. 글 frontmatter에 `published`, `description`, `author`, `tags`를 확인한다.
3. 글 안에서 관련 원문 1개 이상을 자연스럽게 연결한다.
4. `/research`의 주제 묶음에 새 글을 반영할지 판단한다.
5. 필요하면 홈의 최신 글 목록을 갱신한다.
6. 빌드 후 sitemap과 RSS에 포함되는지 확인한다.
7. Google Search Console과 Naver Search Advisor에서 주요 URL 수집을 요청한다.
8. 외부 확산 글은 원문 발행 뒤 별도 요약/해설/문제제기형으로 작성한다.

이 루틴의 목적은 글 수를 늘리는 것이 아니라, 김민조-연구노트-주제-원문 URL의 연결 신호를 누적하는 것이다.

## 향후 분류 확장

글이 10개 이상 쌓였으므로 다음 축을 `/research` 내부 링크 묶음으로 먼저 운영한다.

- 전략 실행
- AI와 조직
- 조직학습
- 가치와 고객
- 이론의 경계조건
- 방법론과 개념모델

글이 30개 이상 쌓이면 별도 인덱스 페이지를 만든다.

- 이론별 인덱스
- 개념모델 인덱스
- 연구 질문 인덱스
- 발행 연도별 인덱스

## AI 검색 대비

- 제목은 질문형 또는 개념형으로 명확하게 쓴다.
- 첫 3문단 안에 글의 핵심 주장과 이론적 위치를 밝힌다.
- 학자명은 가능한 한 원문 병기를 유지한다.
- 각주와 참고문헌은 발행 글에 남긴다.
- 추후 `llms.txt` 또는 연구노트 요약 인덱스가 실질 표준으로 굳으면 별도 추가한다.

## 외부 채널 원칙

- 외부 글의 목적은 원문 대체가 아니라 원문 발견 신호 강화다.
- 원문은 `minjokim.kr`에 먼저 둔다.
- Naver Blog와 Brunch에는 요약, 해설, 문제제기형 글을 올린다.
- 외부 글에는 "김민조 연구노트"와 minjokim.kr 원문 URL을 함께 명시한다.
- 가능한 경우 외부 글에서 관련 minjokim.kr 원문을 1개 이상 추가 연결한다.
- 원문 전문 복사가 필요한 경우에는 원문 출처 표시를 더 강하게 하고, 플랫폼이 canonical을 지원하지 않으면 요약/재작성본을 우선한다.

## 주기적 조정 원칙

- 월 1회 또는 주요 글 3편 발행 후, 검색 노출과 외부 채널 반응을 함께 본다.
- 반응이 있는 주제는 `/research`의 묶음, 홈 문구, 외부 글 제목 전략에 반영한다.
- 단기 유입을 위해 원문 제목과 주장을 과하게 바꾸지 않는다.
- 마케팅 조정은 원문 권위, 저자 엔티티, 내부 링크 구조를 강화하는 방향으로만 한다.
- 외부 채널의 반응이 좋아도 원문 기준점은 항상 `minjokim.kr`로 유지한다.
